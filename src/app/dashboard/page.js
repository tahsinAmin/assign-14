import React from 'react';
import AppNavBar from '../component/AppNavBar';

const Page = async () => {
    return (
        <div>
             <AppNavBar/>
            Dashboard
        </div>
    );
};

export default Page;